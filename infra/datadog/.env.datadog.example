# Copie para /opt/mais-gestao/.env.datadog na VPS (chmod 600).
# Nunca commite a API key real no repositório.

DD_API_KEY=<SUA_API_KEY>
DD_SITE=us5.datadoghq.com
DD_ENV=prod
DD_HOSTNAME=srv1534149

# Usado pelo Agent em container (docker-compose.prod.yml), se preferir essa abordagem.
# Com Agent instalado no host (systemd), estas variáveis vão em /etc/datadog-agent/datadog.yaml.

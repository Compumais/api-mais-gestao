# Validador XML

## Camada 1 — estrutural (v1 no motor: campos)

XML bem formado, encoding, namespaces, tags obrigatórias, cardinalidade, tamanho, tipos, casas decimais, chave e DV: conferir o que o emissor já monta. Schema XSD e assinatura digital: gateway PHP + SEFAZ.

## Camada 2 — não confundir

Documento autorizado (`nfeProc` + `cStat=100`) passou na camada da SEFAZ. Continuar as camadas fiscal e de legislação.
